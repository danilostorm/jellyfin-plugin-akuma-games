name: Build and publish Akuma Games

on:
  push:
    branches: [main]
    paths:
      - 'bootstrap/source.zip.b64.00'
      - '.github/workflows/build-publish.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Decode source
        run: |
          base64 -d bootstrap/source.zip.b64.00 > /tmp/source.zip
          rm -rf /tmp/akuma-src
          mkdir -p /tmp/akuma-src
          unzip -q /tmp/source.zip -d /tmp/akuma-src

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Build plugin
        working-directory: /tmp/akuma-src/jellyfin-plugin-akuma-games-src
        run: |
          dotnet restore Jellyfin.Plugin.AkumaGames.sln
          dotnet build Jellyfin.Plugin.AkumaGames.sln -c Release --no-restore

      - name: Package plugin
        run: |
          mkdir -p repository/package
          cp /tmp/akuma-src/jellyfin-plugin-akuma-games-src/Jellyfin.Plugin.AkumaGames/bin/Release/net9.0/Jellyfin.Plugin.AkumaGames.dll repository/package/
          cd repository/package
          zip -9 ../Jellyfin.Plugin.AkumaGames_0.1.0.0.zip Jellyfin.Plugin.AkumaGames.dll
          cd ../..
          rm -rf repository/package
          CHECKSUM=$(md5sum repository/Jellyfin.Plugin.AkumaGames_0.1.0.0.zip | awk '{print $1}')
          TIMESTAMP=$(date -u '+%Y-%m-%d %H:%M:%S')
          cat > repository/manifest.json <<JSON
          [
            {
              "category": "General",
              "guid": "0fe9988e-21a1-4e59-aed7-4cf2e3858143",
              "name": "Akuma Games",
              "description": "Sincroniza a API Akumanimes como uma biblioteca nativa do Jellyfin.",
              "owner": "danilostorm",
              "overview": "Games nativos no Jellyfin, Android TV e Fire TV",
              "versions": [
                {
                  "checksum": "${CHECKSUM}",
                  "changelog": "Primeira versão com biblioteca nativa Games, capas e metadados.",
                  "targetAbi": "10.11.11.0",
                  "sourceUrl": "https://raw.githubusercontent.com/danilostorm/jellyfin-plugin-akuma-games/main/repository/Jellyfin.Plugin.AkumaGames_0.1.0.0.zip",
                  "timestamp": "${TIMESTAMP}",
                  "version": "0.1.0.0"
                }
              ]
            }
          ]
          JSON

      - name: Publish repository files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add repository/manifest.json repository/Jellyfin.Plugin.AkumaGames_0.1.0.0.zip
          if git diff --cached --quiet; then
            echo "No changes to publish"
            exit 0
          fi
          git commit -m "build: publish Akuma Games plugin v0.1.0.0"
          git push
